# Blueprint Evidence Dev — Iterations and Learnings

| Date | Version/phase | Change or experiment | Why | Result/evidence | Decision or next action |
|---|---|---|---|---|---|
| 2026-08-29 | Phase 0 | Created implementation skeleton and credential-safe preflight | Begin with auditable setup before live workflows | Files created; live checks pending | Complete provider and n8n preflight |
| 2026-08-29 | Phase 0 | Standardized name to `Blueprint Evidence Dev`; selected `blueprint-evidence-dev` technical slug | Keep one recognizable identity across every platform | Local folder and all plans renamed; no old name remains | Use the naming matrix during account setup |
| 2026-08-29 | Architecture | Retained Supabase and Fireworks; deferred LlamaIndex and ElevenLabs from V1 | Each service must add a distinct capability to the critical path | Supabase remains state/auth; Fireworks remains primary inference | Reconsider voice or document ingestion only after core gates pass |
| 2026-08-29 | Architecture | Removed Fireworks from V1; assigned fast, strong and audit roles to Nebius Token Factory | Reduce setup and failure surfaces while retaining structured multi-model behavior | One required inference provider; Fireworks optional later | Smoke-test current Nebius models and choose role IDs |
| 2026-08-29 | Phase 0 | Migrated n8n to named persistent Docker storage and imported a sanitized provider smoke workflow | Protect legacy workflows and establish repeatable provider checks | Restart/integrity counts matched; You.com, Nebius, Pinecone and Supabase connectivity passed | Keep the smoke workflow inactive and complete data-plane/model-role tests |
| 2026-08-29 | Phase 1 | Applied migration `001_foundation.sql` to Supabase | Establish authoritative multi-tenant state, evidence, RLS, storage and atomic routing before agent workflows | 16 tables, 16 RLS-enabled tables, 47 policies, private bucket, 52 transitions, one RPC and 17 indexes all verified | Create two Auth test users, run isolation tests, then build BP-90 and BP-API-01 |
| 2026-08-29 | Phase 1 | Added RLS to the read-only transition lookup after Supabase warned before execution | Remove even intentional exposed-table exceptions | First run was cancelled before execution; corrected transaction then applied cleanly | Keep dashboard warnings as a mandatory pre-commit quality signal |
| 2026-08-29 | Gate A | Ran rollback-only ownership, idempotency and state-machine tests with two Auth users | Prove security and concurrency controls before exposing an n8n webhook | First run found the internal transition bypass remained enabled later in the same transaction; no test rows persisted | Reset the bypass immediately after the guarded update and rerun |
| 2026-08-29 | Gate A | Applied `002_harden_state_guard.sql` and reran the full test | Close the same-transaction direct-update path identified by the test | 9/9 checks passed; cross-user read/write/RPC, stale version, illegal transition, direct update and duplicate run were rejected; cleanup counts were all zero | Begin BP-90 and BP-API-01 |
| 2026-08-29 | BP-90 | Applied `003_record_workflow_failure.sql` and imported `BP-90 Error and Audit` into persistent n8n | Give the global error workflow one narrow, server-only path to record owned failures, dead letters and safe run status | Live workflow ID `bp90ErrorAudit01`; seven nodes; one Error Trigger; inactive; sanitized export; Supabase secret credential intentionally unbound | User privately creates/binds `BP Supabase Error Writer`, then run controlled failure tests |
| 2026-08-29 | BP-90 | Bound `BP Supabase Error Writer`, applied `004_prepare_bp90_failure_test.sql`, and ran private controlled failures | Prove the actual Error Trigger path, classification, audit persistence, dead-letter handling, and safe state transition without exposing a test webhook | BP-90 executions 191 and 193 succeeded; the latest synthetic run became `SAFE_FAILED` with exactly one error, one dead letter, and one audited transition; provider failure was retryable and replay eligible | Keep BP-90 active; keep `BP-TEST-90 Controlled Failure Harness` unpublished; begin BP-API-01 |
| 2026-08-29 | BP-90 learning | Initial test attempts exposed an invalid protected initial-state write, an inactive error workflow, and a CLI/SQLite handoff limitation | Validate failure paths instead of assuming the happy path proves orchestration | Removed protected initial route/node writes; activated the non-public BP-90 Error Trigger; replaced the public-webhook idea with a temporary private schedule and immediately unpublished it after one run | Reuse this activation-safe pattern for future local failure tests |
| 2026-08-29 | Phase 2 provider gate | Added and ran `BP-SETUP-01 Phase 2 Validation` | Prove actual structured outputs and Pinecone data-plane behavior rather than treating reachability as readiness | Nebius Fast and Strong passed immediately; GPT-OSS Audit first exhausted an 80-token budget on reasoning, then passed with low reasoning effort and a 400-token cap; Pinecone upsert/search/delete passed and cleaned its test record | Use Qwen3-30B for fast work, Qwen3-235B for strong synthesis, and GPT-OSS-120B for independent audit; preserve role-specific token budgets |
| 2026-08-29 | Phase 3 Start API | Applied `005_start_blueprint_run.sql`, added request/response/error schemas, and imported inactive `BP-API-01 Start Run` | Establish the authenticated, idempotent entry contract before any research agents | Gate B passed 6/6; imported workflow has 13 nodes, correct public credential bindings, BP-90, no secrets, and remains unpublished | Use Streamlit Auth to obtain a real user JWT, then run the production-webhook success/replay test before permanent publication |
| 2026-08-29 | Phase 3 activation learning | Attempted a temporary CLI publish/restart test of the localhost webhook | Verify the real 400/401 response paths without leaving a test endpoint active | CLI publication did not register the route; both probes returned 404. Cleanup/unpublish succeeded and final export showed `active=false`. n8n also logged a pre-existing Confluence credential warning and a SQLite driver-release shutdown warning, but restarted and loaded the two intended active workflows | Do not claim a webhook pass from this attempt; publish/test through the running editor only after Streamlit JWT plumbing exists |
| 2026-08-29 | Phases 4–5 | Built and imported `BP-CORE-45 Evidence Blueprint` with idea framing, three evidence streams, specialist analyses, deterministic finance, different-family audit, one bounded repair, full/partial synthesis and BP-90 | Deliver the core founder value before Streamlit integration while preserving evidence and safe failure | Import passed; 42-node structural audit found no missing targets or Code-node syntax errors, all roles present, and no external write URLs; workflow remains inactive | Run one signed-in manual test before marking the live gate verified |
| 2026-08-29 | Phase 4–5 data contract | Added migration 006 for idea-specific section status and an owner-scoped dashboard RPC | Preserve the reusable Streamlit shell while replacing session-state progress with durable evidence/state | First attempt rolled back on a SQL grouping error; lateral aggregation fix applied successfully; table/RLS/four policies/authenticated RPC/anonymous denial passed 5/5 | Keep database migrations transactional and treat dashboard warnings/errors as pre-commit gates |
| 2026-08-29 | Phase 4–5 test safety | Declined to stop the shared n8n service and mount its live SQLite volume into a second container for CLI execution | Avoid database race/corruption while other workflows exist | Used import/export and static validation safely; browser execution is blocked only by the expired local n8n UI session | Require one n8n sign-in rather than weakening storage safety |
| 2026-08-30 | Phase 4–5 live failure path | Ran execution `199` after n8n sign-in | Prove the system handles provider errors and does not fabricate a happy answer | You.com rejected generated queries over its 50-word/400-character limit; the workflow completed safely as `HUMAN_REVIEW` with zero invented citations | Cap planned and repair queries deterministically before provider calls |
| 2026-08-30 | Phase 4–5 provider recovery | Capped queries at 45 words/380 characters and reran execution `200` | Verify real research and the bounded repair path | All You.com calls and specialist agents succeeded; the auditor still treated the pre-audit `PENDING` marker as rejection and safely returned `HUMAN_REVIEW` | Remove the placeholder from auditor input and clarify directional versus high-stakes evidence policy |
| 2026-08-30 | Phase 4–5 live acceptance | Preferred full snippets, made the different-family auditor assign first verdicts, and ran execution `201` | Permit useful directional synthesis without weakening WTP/market-size guardrails | Success in 77.648 s; 23 accepted citations across three streams; audit `PASS`; no repair; synthesis completed; exact market size/WTP/conversion/forecast remained unknown | Mark Phases 4–5 live-verified and begin Phase 6 Supervisor/HITL/persistence |
| 2026-08-30 | Phase 6 quality gate | Added an independent seven-rubric Blueprint critic, one bounded revision, evidence-ID allowlist, and final re-critic | Prevent polished but incomplete or newly fabricated Blueprints from passing | Safe fixture first requested repair; reviser attempted unsupported additions; parser removed invented evidence; re-critic returned `QUALITY_FAILED / PARTIAL_COMPLETE` | Preserve best partial output and never allow a second revision |
| 2026-08-30 | Phase 6 Supervisor | Added BP-00 with deterministic scope/input/budget routing and BP-CORE-45/BP-QA-01 handoffs | Make the system choose the next step instead of treating the core as a fixed one-shot workflow | Full live fixture passed with 12 agent/supervisor trace entries and selected `MEMORY_INDEX` after the independent quality gate | Bind production JWT persistence and HITL resume |
| 2026-08-30 | Phase 6 Research Copilot | Added grounded Q&A, phase/next-step intent, confirmation-gated module commands, and out-of-scope denial | Let founders interrogate research without giving the chatbot unrestricted action authority | Grounded answer cited only accepted evidence; first denial probe exposed that the word “research” weakened an action regex; hardened rule then returned `OUT_OF_SCOPE` for “ping/send” | Keep external operational verbs denied even when mixed with research language |
| 2026-08-30 | Phase 6B staged planner | Replaced all-at-once planning with three gate-aware modes and deterministic task locks | Prevent model-created work from bypassing founder decisions | A regression found `RUN_MISSING_RESEARCH` was overwritten by a generic gate fallback; explicit decision precedence fixed it | Test every branch priority, not only every individual condition |
| 2026-08-30 | Phase 6B scheduler fan-out | Added adapter-aware atomic claim, versioned task context, parameterized Stage 1 worker and per-item observation handling | Preserve parallel research and make each result independently observable | Two live tests exposed item collapse in context and observation Code nodes; both were converted to per-item mode | In n8n, verify item counts at every fan-out/fan-in boundary |
| 2026-08-30 | Phase 6 persistence | Applied migrations 007–008 and wired BP-API-01, BP-00, and BP-CHAT-01 to authenticated atomic RPCs | Make run context, final Blueprint, accepted evidence, quality, transitions, commands, and chat durable and owner-isolated | Four Phase 6 tables, RLS, 11 policies, authenticated-only context/persistence functions, and text citation IDs all passed verification | Run one real Streamlit JWT acceptance request; manual fixtures correctly skip persistence |
| 2026-08-30 | Phase 6C canonical-evidence correction | Persisted independently audited Stage 1 evidence in Supabase before projecting it to Pinecone | Temporary search-card IDs could not safely support later retrieval revalidation | Migration 016 installed; `BP-PINE-01` passed integrated-text upsert, semantic match and scoped delete | Keep Pinecone rebuildable and reject any hit that Supabase no longer authorizes |
| 2026-08-30 | Phase 6D Founder Journey memory | Added a confirmed-memory allowlist, founder/project scope, Supabase projection tracking and Mem0 cleanup | Preserve useful cross-session personalization without making Mem0 canonical | `BP-MEM0-01` passed add, scoped search, exact match and scoped cleanup | Evaluate temporal conflicts and cross-user isolation in 6G; expose inspect/correct/delete in Phase 7 |
| 2026-08-30 | Phase 6E2 profile rerun | Added immutable save, deterministic field-to-module impact, durable confirmation, stale-preview rejection and dependency-closed targeted/full reruns | A profile change must not silently rerun work or overwrite the original Blueprint | Migration 017 installed; `BP-RERUN-01` targeted-plan fixture passed with four affected typed tasks and no test-mode write | Run authenticated save/preview/approve/cancel/replay cases through Streamlit JWT |
| 2026-08-30 | Phase 6F resilience | Added a shared caught-failure controller, owner-scoped observability RPCs, durable eval tables and 15 failure injections | Expected tool failures need bounded routing before the global BP-90 crash boundary | Migration 018 installed; `BP-RESILIENCE-01` published; live matrix passed 15/15 | Surface observability and pending decisions in Streamlit |
| 2026-08-30 | Phase 6G regression learning | Executed actual exported n8n Code nodes plus workflow/security contracts | A green happy path did not prove guard precedence or scope denial | First run 60/64 exposed one regex escaping defect and three overwritten guards; implementation fixed; final suite 66/66 | Keep these fixtures frozen and run them after every workflow change |
| 2026-08-30 | Phase 7 authenticated vertical slice | Preserved the Blueprint V1 UI in `blueprint-v2`, added Supabase auth/token refresh/logout, RLS project recovery, authenticated selected-research intake, live status and checkpoint controls | Durable ownership and task completion require more than a same-session mock | Frontend contracts pass 7/7; orchestration suite passes 68/68; published webhook returns expected 422/400/401 safety envelopes | Add private Streamlit settings, prove real-JWT start and two-user isolation, then bind dynamic sections/reruns |
| 2026-08-30 | Phase 7 UX and research-quality correction | Rebuilt the founder workspace as a live three-pane interface, made multi-select onboarding persistent, added explanatory verdict and gate states, and upgraded the Stage 1 specialist output contract | The prior UI hid progress and rendered every output generically; the prior competitor prompt could mistake directories/research tools for competitors and repeated one limitation per source | Browser fixture verified live/queued/locked/error-ready navigation, competitor matrix, goal-aware title, centered Gate 1 dialog, sticky section chat and collapsed evidence rails; Python contracts remain 11/11; the updated Stage 1 workflow imported successfully into local n8n | Run one fresh real idea to acceptance-check richer Stage 1 output; historical runs remain immutable and need an approved rerun |
